html
  head
    title= title
    style.
        body {
            background-color: rgba(40, 40, 40, 0.85);
            color: rgb(240, 20, 20);
            margin-top: 5vh;
            margin-left: 10vw;
        }

        a {
            color: rgb(240, 20, 20);
            font-size: 2em;
        }

        h1 {
            font-size:3em;
        }

        p {
            font-size: 1.5em;
        }

        iframe{
            border: none;
        }

  body
    a(href="//localhost:3000") <= Back
    h1= header
    h2= subheader
    p= details
    iframe(calssName="video" width='900', height='450', src=link)= link
